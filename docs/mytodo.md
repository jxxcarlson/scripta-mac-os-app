- Replies in the AI chat window have a "Copy" button.  Add new button "File".
  Add also a text field for a file title.  When "File" is clicked a file
  with the given name and the givenr reply contents is created in <Vault>/Inbox.
  
- Rename the [Back] button to [Prev], Add cmd-[ as a shortcut to the [Previous] button. Also, add a [Next] button which is inverse to [Prev], and and a shortcut cmd-] to Next]


---

- Currently file names in the file tree display are prefixed with "-".  Let's change that to a small solid dark blue icon that looks like a file.  It should be vertically aligned to the position of the first nonspace character of the enclosing folder.  Title should be displayed in full and in hanging indent form.

- Rename the [shell 1] button to [<Vault>]. On load or reload, the app should execute "cd Vault", then execute "<ai-name>".  In the current setup, <ai-name> is "claude" but in general that is not true. Can this agent be deduced from the data currently in the table, or do we need a new column?


